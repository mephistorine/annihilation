import { CellDefDirective } from './cell-def.directive'

describe('CellColumnDefDirective', () => {
  it('should create an instance', () => {
    const directive = new CellDefDirective();
    expect(directive).toBeTruthy();
  });
});
