import { HeaderCellDefDirective } from './header-cell-def.directive'

describe('HeaderCellDefDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderCellDefDirective();
    expect(directive).toBeTruthy();
  });
});
