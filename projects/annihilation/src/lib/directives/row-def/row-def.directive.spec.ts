import { RowDefDirective } from './row-def.directive'

describe('RowDefDirective', () => {
  it('should create an instance', () => {
    const directive = new RowDefDirective();
    expect(directive).toBeTruthy();
  });
});
